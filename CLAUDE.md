# CRITICAL: README Sync

ALWAYS update README.md when modifying skills or workflow. Ensure docs stay synced.

# Timestamps

When updating timestamps in files (e.g., GLOSSARY.yaml metadata.last_updated), use:
```bash
date -u +"%Y-%m-%dT%H:%M:%SZ"
```
Do NOT assume current date - always run command to get accurate UTC timestamp.
